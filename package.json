{
  "name": "simpleworkouts",
  "version": "1.0.0",
  "description": "Fast, minimalist workout logging from free-form text - fully client-side with localStorage",
  "scripts": {
    "dev": "cd client && npm run dev",
    "start": "cd client && npm start",
    "build": "cd client && npm run build",
    "test": "cd client && npm test",
    "install": "cd client && npm install"
  },
  "keywords": [
    "workout",
    "fitness",
    "logging",
    "react",
    "typescript",
    "localStorage"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^30.0.0"
  }
}
